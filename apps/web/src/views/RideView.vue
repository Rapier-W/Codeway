<script setup lang="ts">import { ref } from 'vue'; import RideFallback from '../components/RideFallback.vue'; const plate=ref(''); const model=ref(''); const color=ref(''); const saved=ref(false); function save(){saved.value=!!plate.value.trim()}</script>
<template><main class="page"><div class="eyebrow">出行准备</div><h1>车辆信息</h1><div class="detail-card"><p>请先阅读：同路行不提供司机端、实时定位或自动行车守护。车辆信息仅用于同行识别。</p><label>车牌号<input v-model="plate" required placeholder="粤A·12345" /></label><label>车型（可选）<input v-model="model" placeholder="如：轿车" /></label><label>颜色（可选）<input v-model="color" placeholder="如：白色" /></label><button class="primary-button" :disabled="!plate.trim()" @click="save">{{saved?'已保存':'保存车辆信息'}}</button></div><RideFallback phone="13800138000" text="大学城南门 → 火车站" /></main></template>
